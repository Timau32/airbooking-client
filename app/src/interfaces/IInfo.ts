export default interface IInfo {
  id: number;
  title: string;
  category: string;
  content: string;
  slug: string;
  createdAt: string; // ISO формат даты
  updatedAt: string; // ISO формат даты
}
